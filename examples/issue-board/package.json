{
  "name": "issue-board-example",
  "version": "1.0.0",
  "private": true,
  "description": "Kanban-style issue tracker demonstrating the Zebric widget system (drag-and-drop, inline column rename, card toggles)",
  "scripts": {
    "dev": "zebric-engine --blueprint=blueprint.toml --port=3000",
    "seed": "bash seed.sh",
    "build": "echo 'No build step needed - runtime engine'"
  },
  "dependencies": {
    "@zebric/runtime-node": "workspace:*",
    "@zebric/cli": "workspace:*"
  }
}
